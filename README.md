
  # Barra RikiWohl

  This is a code bundle for Barra RikiWohl. The original project is available at https://www.figma.com/design/eXAtiSwcfvm2rSyHKnxi3l/Barra-RikiWohl.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  